# Maintenance Commands

This directory is reserved for repository and local-state maintenance commands. Product runs start through `scripts.run`; durable background side effects run through `scripts.workers`.
